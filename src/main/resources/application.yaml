server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: multi
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:26379}

app:
  instance-id: ${INSTANCE_ID:local}

device:
  simulator:
    interval-ms: ${DEVICE_SIMULATOR_INTERVAL_MS:30}

